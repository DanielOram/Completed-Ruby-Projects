/*
  <li role="presentation" class="active"><a href="<%= root_path%>">Photogram</a></li>
  <li role="presentation"><a href="<%= new_post_path%>">New Post</a></li>
  <li role="presentation"><a href="#">Login</a></li>
  <li role="presentation"><a href="#">Register</a></li>
*/

/*
var url = window.location;
// Will only work if string in href matches with location
$('ul.nav.nav-tabs a[href="'+ url +'"]').parent().addClass('active');

// Will also work for relative and absolute hrefs
$('ul.nav.nav-tabs a').filter(function() {
    return this.href == url;
}).parent().addClass('active');
*/

//if (str.toLowerCase().indexOf("yes") >= 0)
/*
//simple scrpit to add active class to active navbar
$(document).ready(function () {
	alert();
	var t = $('div').text();
	alert(t);
	if (t == 'new'){
		alert('yass');
		alert('blah');
		$('li#'+t).addClass('active');
	}
		
    $('ul.nav.nav-tabs > li').click(function (e) {
		alert();
        //e.preventDefault();
        $('ul.nav.nav-tabs > li').removeClass('active');
    });  
		/*
		if (url.toLowerCase().indexOf("yes") >= 0) {
            document.getElementById(url).removeClass('active');
            $('').addClass('active'); 
		}
		alert(url);
        $('ul.nav.nav-tabs > li').click(function (e) {
			alert();
            //e.preventDefault();
            $('ul.nav.nav-tabs > li').removeClass('active');
            $(this).addClass('active');                
        });  
		*/ 
/*         
    });
*/
